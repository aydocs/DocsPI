name: DocsPI v5.0.0

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-divert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: false

      - name: Build docspi-divert for Windows
        run: |
          cd docspi-divert
          rm -f go.sum
          GONOSUMCHECK=* GONOSUMDB=* GOFLAGS=-mod=mod go mod tidy
          GOOS=windows GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o ../docspi-divert.exe .
          echo "Build OK"
          ls -lh ../docspi-divert.exe

      - name: Upload divert binary
        uses: actions/upload-artifact@v4
        with:
          name: docspi-divert
          path: docspi-divert.exe
          retention-days: 1

  build-proxy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: false

      - name: Cross-compile SpoofDPI v1.2.1 for Windows
        run: |
          curl -L "https://github.com/xvzc/SpoofDPI/archive/refs/tags/v1.2.1.tar.gz" -o spoofdpi.tar.gz
          tar xzf spoofdpi.tar.gz
          SPOOFDIR=$(find . -maxdepth 1 -type d | grep -i spoof | head -1)
          echo "Klasör: $SPOOFDIR"
          cp scripts/patch-spoofdpi.sh "$SPOOFDIR/"
          cd "$SPOOFDIR"
          bash patch-spoofdpi.sh
          GOOS=windows GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o ../docspi-proxy.exe ./cmd/spoofdpi
          echo "Build OK"
          ls -lh ../docspi-proxy.exe

      - name: Upload proxy binary
        uses: actions/upload-artifact@v4
        with:
          name: docspi-proxy
          path: docspi-proxy.exe
          retention-days: 1

  build-tauri:
    needs: [build-proxy, build-divert]
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Get version from package.json
        id: version
        shell: bash
        run: echo "VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Download proxy binary
        uses: actions/download-artifact@v4
        with:
          name: docspi-proxy
          path: spoofdpi/

      - name: Download divert binary
        uses: actions/download-artifact@v4
        with:
          name: docspi-divert
          path: divert/

      - name: Copy binaries to tauri binaries
        shell: bash
        run: |
          mkdir -p src-tauri/binaries
          cp spoofdpi/docspi-proxy.exe src-tauri/binaries/docspi-proxy.exe
          cp spoofdpi/docspi-proxy.exe "src-tauri/binaries/docspi-proxy-x86_64-pc-windows-msvc.exe"
          cp divert/docspi-divert.exe src-tauri/binaries/docspi-divert.exe
          cp divert/docspi-divert.exe "src-tauri/binaries/docspi-divert-x86_64-pc-windows-msvc.exe"
          ls -lh src-tauri/binaries/

      - name: Update proxy icon
        shell: bash
        run: node change-icon.js || echo "Icon update skipped"

      - name: Download Npcap installer
        shell: bash
        run: |
          curl -L --retry 3 --max-time 120 \
            "https://npcap.com/dist/npcap-1.79.exe" \
            -o "src-tauri/binaries/npcap-installer.exe" \
            --user-agent "Mozilla/5.0"
          ls -lh src-tauri/binaries/npcap-installer.exe

      - name: Download WinDivert
        shell: bash
        run: |
          curl -L --retry 3 --max-time 120 \
            "https://github.com/basil00/WinDivert/releases/download/v2.2.0/WinDivert-2.2.0-A.zip" \
            -o windivert.zip
          unzip -o windivert.zip
          cp WinDivert-2.2.0-A/x64/WinDivert.dll src-tauri/binaries/WinDivert.dll
          cp WinDivert-2.2.0-A/x64/WinDivert64.sys src-tauri/binaries/WinDivert64.sys
          ls -lh src-tauri/binaries/WinDivert.dll src-tauri/binaries/WinDivert64.sys

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v${{ steps.version.outputs.VERSION }}
          releaseName: 'DocsPI v${{ steps.version.outputs.VERSION }}'
          releaseBody: 'Last Update'
          releaseDraft: false
          prerelease: false

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: DocsPI-Windows
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/msi/*.msi
          retention-days: 7
